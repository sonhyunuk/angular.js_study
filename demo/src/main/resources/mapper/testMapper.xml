<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.example.demo.mapper.testMapper">
 <select id="getUserInfo" resultType="TestDto">
	  	SELECT
			 user_num		AS userNum
			,user_id		AS userId
		FROM
			tb_test;
    </select>
    </mapper>